spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/members?serverTimezone=Asia/Seoul
    username: root
    password: 1q2w3e4r!
    #만들어진 sql 보여줌

  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create
    show-sql: true
    format-sql: true
  redis:
    host: localhost
    port: 6379

oauth:
  kakao:
    client_id: 655ca6aff24fe1c0ba1df12686168206 # REST API 키
    redirect_uri: http://localhost:3000/oauth/redirected/kakao
    client_secret: Hz6GebgdBzBnbie0aMTiz3IekZprsXNL # Client Secret 키
    scope: account_email

jwt:
  secretKey: d12jdksakdksddk1k2k3dda12dghjssaasdsv34789yhiuFDSIUFGYDTE5rssaasdsv34789yhiuFDSIUFGYDTE5rssaasdsv34789yhiuFDSIUFGYDTE5rv34789yhiuFDSIUFGYDTE5rssaasdsv34789yhiuFDSIUFGYDTE5r

  access:
    expiration: 3600000 # 1시간(60분) (1000L(ms -> s) * 60L(s -> m) * 60L(m -> h))
    header: Authorization

  refresh:
    expiration: 1209600000 #  (1000L(ms -> s) * 60L(s -> m) * 60L(m -> h) * 24L(h -> 하루) * 14(2주))
    header: Authorization-refresh

server:
  port : 8080
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

logging:
  level:
    com.board : DEBUG


